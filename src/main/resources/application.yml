spring:
  application:
    name: did_career_certification
  messages:
    encoding: UTF-8

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: update
      format_sql: true
    show-sql: true
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

  config:
    import: application-secret.yml

jwt:
  header: Authorization
  secret-key: TKwoaudc201dowoaudroQkrclspwlsWKfhdpdldkdlwhaQKfflgofkgwehewrheherherhsfhsdgalwlpwqpfwkqkpfpqfqkpqwetrwejktj
  expiration-time: 1800000 # 1000(ms) * 60(s) * 30(m) = 30분

ganache:
  RPC_URL: http://127.0.0.1:7545